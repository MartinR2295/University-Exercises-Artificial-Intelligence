import { ObstacleAvoidanceRule } from './obstacle-avoidance-rule.model';

describe('ObstacleAvoidanceRule', () => {
  it('should create an instance', () => {
    expect(new ObstacleAvoidanceRule()).toBeTruthy();
  });
});
